<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Shorten Url</title>
    <style>
        body {
            font-family: Gilroy;
        }

        table {
            margin-top: 40px;
            width: 60%;
            border-collapse: collapse;
        }

        th,
        td {
            padding: 12px;
            border: 1px solid #ddd;
        }

        th {
            background-color: #f2f2f2;
            font-weight: bold;
            text-align: left;
        }

        td {
            background-color: #fff;
        }
    </style>
</head>

<body>
    <h1>URL Shortner</h1>
    <form method="post" action="/url">
        <label for="url">URL : </label>
        <input type="text" name="url" id="url" placeholder="Enter orginal url..">
        <button type="submit">Generate</button>
    </form>

    <% if (locals.urls) { %>      
        <table>
            <thead>
                <th>S.NO</th>
                <th>Short Url</th>
                <th>Orginal Url</th>
                <th>Total Clicks</th>
            </thead>
            <tbody>
                <% urls.forEach((url,index)=> { %>
                    <tr>
                        <td><%- index + 1 %></td>
                        <td>http://localhost:8001/url/<%- url.shortId %></td>
                        <td><%- url.redirectUrl %></td>
                        <td><%- url.visitHistory.length %></td>
                    </tr>
                    <% }) %>
            </tbody>
        </table>
    <% } %>
</body>

</html>